ps 

命令简介:
   汇报系统中当前存在的进程信息，是快照。
用法:
	ps [options]
详细用法：
	ps 展示一组活动进程的信息。如果想实时跟踪进程信息，则使用top命令。
	此版本的ps接受三种选项：
	1 UNIX 选项，必须连在一起，并紧跟在-后面（ps -aux）
	2 BSD选项， 必须连在一起，不能有-（ps aux）
	3 GNU 长选项， 紧跟两个-（ps --aux）

	不同类型的选项可以自由组合，但是可能会出现冲突。有很多同义的选项，源于	      不同的标准的ps实现，但是彼此是可以兼容的。注意"ps -aux"和"ps aux"是有区别的。POSIX 和UNIX 标准要求“ps -aux”打印所有属于用户"x"的进程,以及-a选项的进程。如果用户x不存在，那么ps会解释为命令"ps aux"，并输出一个警告。这种行为的设计是为了从旧版脚本和习惯过渡。它是不稳定的，随时可能变化，因此不应依赖它。

	默认情况下，ps选择当前有效用户和当前终端调用者。它现实进程ID，进程终端(tty),累计的cpu时间，单位hh:mm::ss 格式，以及可执行程序名称。输出没有排序。

	在BSD风格的选项下，会

